

public class States
{
    public enum GameState
    {
        OnStart,
        OnPlay,
        OnGameOver
    }

    public enum BallMovementState
    {
        Patrol,
        Left,
        Right
    }
}
